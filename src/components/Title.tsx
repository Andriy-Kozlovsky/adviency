import {Typography} from "@mui/material";

const Title = () => {
  return (
    <Typography
      component="h1"
      sx={{fontFamily: "Mountains of Christmas", textAlign: "center"}}
      variant="h3"
    >
      REGALOS:
    </Typography>
  );
};

export default Title;
